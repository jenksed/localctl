package main

func dockerInvestigationExerciseCatalog() []exercise {
	return []exercise{
		infraExact("docker-image-container-v1", "Image versus container", "docker", "easy", "Tests the immutable-template versus running-instance boundary.", "Is a Docker image the same thing as a running container? Reply exactly YES or NO.", "NO"),
		infraExact("docker-exited-v1", "Exited container state", "docker", "easy", "Tests container lifecycle interpretation.", "`docker ps -a` shows STATUS Exited (1). Is the container currently running? Reply exactly YES or NO.", "NO"),
		infraExact("docker-port-publish-v1", "Published port", "docker", "easy", "Tests host/container port mapping semantics.", "`-p 8080:80` publishes host port 8080 to container port 80. Reply exactly YES or NO.", "YES"),
		infraExact("docker-expose-v1", "EXPOSE semantics", "docker", "medium", "Tests that EXPOSE is metadata rather than host publication.", "Does Dockerfile `EXPOSE 80` by itself publish port 80 on the host? Reply exactly YES or NO.", "NO"),
		infraExact("docker-bind-v1", "Bind mount", "docker", "easy", "Tests host path visibility inside a container.", "`-v /host/data:/data` makes /host/data available in the container at /data. Reply exactly YES or NO.", "YES"),
		infraExact("docker-volume-v1", "Named volume persistence", "docker", "easy", "Tests storage lifetime beyond a container instance.", "Can a named Docker volume survive deletion of the container that used it? Reply exactly YES or NO.", "YES"),
		infraExact("docker-layer-cache-v1", "Build cache", "docker", "medium", "Tests Dockerfile layer invalidation reasoning.", "If an early Dockerfile COPY changes, can later cached layers that depend on it be invalidated? Reply exactly YES or NO.", "YES"),
		infraExact("docker-copy-order-v1", "Dependency cache pattern", "docker", "medium", "Tests common dependency-cache optimization reasoning.", "For a Node build, copying package manifests and installing dependencies before copying frequently changing app source usually improves cache reuse. Reply exactly YES or NO.", "YES"),
		infraExact("docker-entrypoint-cmd-v1", "ENTRYPOINT and CMD", "docker", "medium", "Tests default-argument semantics.", "With exec-form ENTRYPOINT and CMD, CMD can provide default arguments to ENTRYPOINT. Reply exactly YES or NO.", "YES"),
		infraExact("docker-pid1-v1", "Container PID 1", "docker", "medium", "Tests signal/reaping responsibilities of the first process in a container.", "The main container process runs as PID 1 in its PID namespace. Reply exactly YES or NO.", "YES"),
		infraExact("docker-sigterm-v1", "docker stop signal", "docker", "medium", "Tests graceful-stop behavior before force kill.", "By default, `docker stop` first sends a termination signal and waits before force killing. Reply exactly YES or NO.", "YES"),
		infraExact("docker-health-v1", "Container healthcheck", "docker", "easy", "Tests distinction between running and healthy.", "A container can be running while Docker HEALTHCHECK reports unhealthy. Reply exactly YES or NO.", "YES"),
		infraExact("docker-restart-v1", "Restart policy", "docker", "medium", "Tests that restart configuration changes lifecycle behavior.", "A container with restart policy `always` exits unexpectedly. Is Docker configured to attempt restarting it? Reply exactly YES or NO.", "YES"),
		infraExact("docker-logs-v1", "Docker logs scope", "docker", "easy", "Tests standard stdout/stderr log retrieval.", "For a default logging setup, choose the command family to inspect a container's stdout/stderr: DOCKER_LOGS or DOCKER_CP. Reply with one label.", "DOCKER_LOGS"),
		infraExact("docker-inspect-v1", "Docker inspect", "docker", "easy", "Tests choosing structured metadata inspection.", "Which command family provides structured container metadata such as mounts and network settings: DOCKER_INSPECT or DOCKER_RMI? Reply with one label.", "DOCKER_INSPECT"),
		infraExact("docker-network-dns-v1", "Compose service DNS", "docker", "medium", "Tests service-name resolution on a shared Compose network.", "Two Compose services share the default network. Can one normally resolve the other by service name? Reply exactly YES or NO.", "YES"),
		infraExact("docker-localhost-v1", "Container localhost boundary", "docker", "medium", "Tests localhost meaning inside a container.", "Inside container A, does 127.0.0.1 normally refer to container B? Reply exactly YES or NO.", "NO"),
		infraExact("docker-host-port-v1", "Host publication need", "docker", "medium", "Tests when inter-container communication needs host publishing.", "Two containers on the same user-defined network communicate directly via container port. Must that port also be published to the host? Reply exactly YES or NO.", "NO"),
		infraExact("docker-env-v1", "Container environment", "docker", "easy", "Tests runtime environment injection.", "`docker run -e MODE=prod ...` provides MODE=prod to the container process environment. Reply exactly YES or NO.", "YES"),
		infraExact("docker-secret-image-v1", "Secret in image layer", "docker", "medium", "Tests why build-time secret copying is risky.", "A Dockerfile COPYs an API key into an image and deletes it in a later RUN layer. Can the secret still exist in earlier image layers? Reply exactly YES or NO.", "YES"),
		infraExact("docker-root-user-v1", "Container root boundary", "docker", "medium", "Tests recognition that container root is still a privilege concern.", "Running an application as root inside a container is automatically equivalent to least privilege. Reply exactly YES or NO.", "NO"),
		infraExact("docker-oom-v1", "Container OOM", "docker", "medium", "Tests memory-limit failure classification.", "A container exceeds its enforced memory limit and is terminated by the kernel. Reply exactly OOM or DISK.", "OOM"),
		infraExact("docker-cpu-limit-v1", "CPU quota", "docker", "medium", "Tests that CPU limits can throttle without killing the container.", "Can a CPU-constrained container remain healthy but run more slowly due to throttling? Reply exactly YES or NO.", "YES"),
		infraExact("docker-disk-layer-v1", "Writable layer growth", "docker", "medium", "Tests ephemeral writable-layer storage reasoning.", "An app writes large files inside the container filesystem with no volume. Can the container writable layer grow? Reply exactly YES or NO.", "YES"),
		infraExact("docker-rm-data-v1", "Container deletion data", "docker", "medium", "Tests persistence boundary for unmounted writable data.", "Data written only to a container writable layer is expected to survive `docker rm` of that container. Reply exactly YES or NO.", "NO"),
		infraExact("docker-build-context-v1", "Build context", "docker", "medium", "Tests why large build contexts matter.", "Can an unnecessarily large Docker build context increase transfer/build overhead? Reply exactly YES or NO.", "YES"),
		infraExact("docker-ignore-v1", ".dockerignore", "docker", "easy", "Tests build-context exclusion purpose.", "Which file excludes paths from Docker build context: .dockerignore or .gitkeep? Reply exactly .dockerignore.", ".dockerignore"),
		infraExact("docker-compose-depends-v1", "Compose dependency readiness", "docker", "medium", "Tests avoiding the assumption that startup order proves service readiness.", "Does basic Compose `depends_on` ordering by itself prove a dependency's application endpoint is ready? Reply exactly YES or NO.", "NO"),
		infraExact("docker-multistage-v1", "Multi-stage build", "docker", "medium", "Tests final-image artifact selection.", "Can a multi-stage Docker build leave compilers/build tools out of the final runtime image? Reply exactly YES or NO.", "YES"),
		infraContains("docker-log-triage-v1", "Docker startup failure", "docker", "hard", "Tests extracting the relevant failure from container startup evidence.", "Evidence: container exits code 1; logs say `bind: address already in use`; host port 8080 is already listening. In one sentence identify the strongest failure and boundary.", "port", "bind"),
		infraManual("docker-container-triage-v1", "Docker container triage", "docker", "hard", "Tests investigation sequencing for an exited application container.", "A Dockerized API immediately exits after deployment. Give the first six checks in order using container state, logs, exit code, configuration, mounts, and resources. Maximum 180 words."),
		infraManual("docker-network-triage-v1", "Docker network triage", "docker", "hard", "Tests investigation of connectivity across host/container/service boundaries.", "Container web cannot reach container api. Both are supposedly in the same Compose project. Give an evidence-first network investigation plan that distinguishes DNS, network membership, listening address, target port, and host-published port confusion. Maximum 190 words."),
		infraManual("docker-build-triage-v1", "Docker build optimization", "docker", "hard", "Tests practical build diagnosis rather than generic optimization advice.", "A Docker build takes 8 minutes after every one-line source change even though dependencies rarely change. Give a bounded investigation and likely Dockerfile restructuring approach, including what evidence would confirm cache invalidation. Maximum 170 words."),
	}
}
