interface:
  display_name: "Localctl"
  short_description: "Repo-specific patterns and workflows for localctl"
  default_prompt: "Use the localctl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true